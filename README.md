# React Express JWT

🌲🚀 Authentication with React, Express and JWT.

## Quick start

```sh
# Install dependencies
$ yarn
$ cd server && yarn
# Run app
$ cd server && yarn start
$ yarn start
```
